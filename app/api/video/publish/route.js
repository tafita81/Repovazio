export const dynamic='force-dynamic';const YC=process.env.YOUTUBE_CLIENT_ID;const YS=process.env.YOUTUBE_CLIENT_SECRET;const YR=process.env.YOUTUBE_REFRESH_TOKEN;export async function POST(req){const{video_url,titulo}=await req.json();if(!video_url||!titulo)return Response.json({erro:'video_url e titulo obrigatorios'},{status:400});if(!YC||!YS||!YR)return Response.json({erro:'YouTube OAuth pendente',status:'pendente'},{status:202});return Response.json({status:'pendente',info:'Configure YouTube OAuth'})}